# Copyright 2026 上海如静知华信息科技有限公司 · https://www.zhuatech.cn/
services:
  mysql:
    image: mysql:8.4
    environment: {MYSQL_DATABASE: zhuatech_cronops, MYSQL_USER: zhuatech, MYSQL_PASSWORD: "${MYSQL_PASSWORD:?Set MYSQL_PASSWORD}", MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD:?Set MYSQL_ROOT_PASSWORD}"}
    ports: ["3309:3306"]
    volumes: ["cronops_mysql:/var/lib/mysql", "./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro"]
volumes: {cronops_mysql: {}}
